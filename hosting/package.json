{
  "name": "hosting-placeholder",
  "version": "0.1.0",
  "private": true,
  "comment": "This directory can be used for static assets if deploying via firebase.json with source 'hosting', but the main Next.js app is at the project root."
}
